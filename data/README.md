This directory is used for generated or user-provided multivariate time-series datasets.
The default pipeline generates a research-oriented synthetic dataset with cross-domain coupling and concept drift.
When `Banglore_traffic_Dataset_raw.csv` and `AirQualityUCI.csv` exist in the project root, the loader auto-generates `urban_multivariate_timeseries.csv` here for the training pipeline.
